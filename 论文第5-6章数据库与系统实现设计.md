# 第五章 数据库设计

## 5.2 数据库概念结构设计

### 5.2.1 E-R图设计
本系统围绕“用户下单-商家接单-平台监管”的业务主线进行建模，核心实体包括：用户、商家、菜品分类、菜品信息、购物车、订单、订单明细、收货地址、评价、管理员。

从业务流程角度看，用户先浏览商家和菜品，再将菜品加入购物车并提交订单，订单在支付后由商家处理，完成后用户可进行评价；管理员负责平台级管理与审核。E-R 图可按下述关系绘制：

- 用户（User）与收货地址（Address）：一对多。
- 用户（User）与购物车（Cart）：一对多。
- 用户（User）与订单（Order）：一对多。
- 商家（Merchant）与菜品信息（Dish）：一对多。
- 菜品分类（Category）与菜品信息（Dish）：一对多。
- 订单（Order）与订单明细（OrderItem）：一对多。
- 菜品信息（Dish）与订单明细（OrderItem）：一对多。
- 用户（User）与评价（Review）：一对多。
- 菜品信息（Dish）与评价（Review）：一对多。
- 商家（Merchant）与评价（Review）：一对多（可通过菜品或订单关联）。

### 5.2.2 实体及其关系分析
1. **用户实体**：记录平台注册用户基础信息与状态，用于鉴权、下单、地址管理等。
2. **商家实体**：记录店铺经营主体信息、营业状态、审核状态。
3. **菜品分类实体**：用于商家内菜品分组与前端分类展示。
4. **菜品信息实体**：记录菜品名称、价格、库存、图片、上下架状态等。
5. **购物车实体**：记录用户待结算商品，支持增删改查。
6. **订单实体**：记录订单主信息（金额、状态、支付方式、地址等）。
7. **订单明细实体**：记录订单中每个菜品的快照信息（单价、数量、小计）。
8. **收货地址实体**：保存用户常用地址与默认地址。
9. **评价实体**：记录用户对订单/菜品/商家的评分与文字评价。
10. **管理员实体**：平台后台账号，负责审核、分类维护、公告发布和监督。

---

## 5.3 数据库逻辑结构设计

### 5.3.1 用户表设计（`user`）
- 功能：存储用户注册登录信息与基础资料。
- 关键点：手机号/邮箱唯一、密码加密存储、状态可控（启用/禁用）。

### 5.3.2 商家表设计（`merchant`）
- 功能：存储商家认证信息、店铺信息与营业状态。
- 关键点：审核字段、营业时间、起送价、配送费。

### 5.3.3 菜品分类表设计（`dish_category`）
- 功能：菜品分类管理。
- 关键点：支持排序字段、是否启用。

### 5.3.4 菜品信息表设计（`dish`）
- 功能：存储菜品详情。
- 关键点：价格精度、库存约束、上下架状态、商家归属。

### 5.3.5 购物车表设计（`cart_item`）
- 功能：保存用户购物车商品。
- 关键点：同用户同菜品唯一约束，避免重复行。

### 5.3.6 订单表设计（`orders`）
- 功能：记录订单主信息。
- 关键点：订单编号唯一、状态机字段、金额字段冗余（商品总额、配送费、实付金额）。

### 5.3.7 订单明细表设计（`order_item`）
- 功能：记录订单中每个菜品条目。
- 关键点：保存菜品下单时快照，防止后续菜品变更影响历史订单。

### 5.3.8 收货地址表设计（`address`）
- 功能：用户收货地址维护。
- 关键点：默认地址唯一性（同一用户仅一个默认地址）。

### 5.3.9 评价表设计（`review`）
- 功能：用户评价管理。
- 关键点：评分范围约束（1~5），一订单可限制一次有效评价。

### 5.3.10 管理员表设计（`admin`）
- 功能：后台管理员登录与权限标识。
- 关键点：角色字段、状态字段、最后登录信息。

---

## 5.4 数据表字段设计说明
以下为核心字段建议（以 MySQL 8.0 为例）：

### 1）用户表 `user`
- `id` BIGINT PK AUTO_INCREMENT：用户主键。
- `username` VARCHAR(50) UNIQUE NOT NULL：用户名。
- `password_hash` VARCHAR(255) NOT NULL：密码哈希。
- `phone` VARCHAR(20) UNIQUE：手机号。
- `email` VARCHAR(100) UNIQUE：邮箱。
- `avatar` VARCHAR(255)：头像地址。
- `status` TINYINT NOT NULL DEFAULT 1：1启用、0禁用。
- `created_at` DATETIME NOT NULL。
- `updated_at` DATETIME NOT NULL。

### 2）商家表 `merchant`
- `id` BIGINT PK AUTO_INCREMENT。
- `merchant_name` VARCHAR(100) NOT NULL。
- `contact_phone` VARCHAR(20) NOT NULL。
- `address` VARCHAR(255) NOT NULL。
- `license_no` VARCHAR(64) UNIQUE NOT NULL。
- `min_order_amount` DECIMAL(10,2) DEFAULT 0.00。
- `delivery_fee` DECIMAL(10,2) DEFAULT 0.00。
- `business_status` TINYINT DEFAULT 1：1营业中，0休息。
- `audit_status` TINYINT DEFAULT 0：0待审，1通过，2驳回。
- `created_at` DATETIME NOT NULL。
- `updated_at` DATETIME NOT NULL。

### 3）菜品分类表 `dish_category`
- `id` BIGINT PK AUTO_INCREMENT。
- `merchant_id` BIGINT NOT NULL FK -> `merchant.id`。
- `category_name` VARCHAR(50) NOT NULL。
- `sort` INT DEFAULT 0。
- `status` TINYINT DEFAULT 1。
- `created_at` DATETIME NOT NULL。

### 4）菜品信息表 `dish`
- `id` BIGINT PK AUTO_INCREMENT。
- `merchant_id` BIGINT NOT NULL FK -> `merchant.id`。
- `category_id` BIGINT NOT NULL FK -> `dish_category.id`。
- `dish_name` VARCHAR(100) NOT NULL。
- `price` DECIMAL(10,2) NOT NULL。
- `stock` INT NOT NULL DEFAULT 0。
- `description` TEXT。
- `image_url` VARCHAR(255)。
- `status` TINYINT DEFAULT 1：1上架，0下架。
- `created_at` DATETIME NOT NULL。
- `updated_at` DATETIME NOT NULL。

### 5）购物车表 `cart_item`
- `id` BIGINT PK AUTO_INCREMENT。
- `user_id` BIGINT NOT NULL FK -> `user.id`。
- `dish_id` BIGINT NOT NULL FK -> `dish.id`。
- `quantity` INT NOT NULL DEFAULT 1。
- `unit_price` DECIMAL(10,2) NOT NULL。
- `created_at` DATETIME NOT NULL。
- `updated_at` DATETIME NOT NULL。
- 唯一约束：`UNIQUE(user_id, dish_id)`。

### 6）订单表 `orders`
- `id` BIGINT PK AUTO_INCREMENT。
- `order_no` VARCHAR(64) UNIQUE NOT NULL。
- `user_id` BIGINT NOT NULL FK -> `user.id`。
- `merchant_id` BIGINT NOT NULL FK -> `merchant.id`。
- `address_id` BIGINT NOT NULL FK -> `address.id`。
- `goods_amount` DECIMAL(10,2) NOT NULL。
- `delivery_fee` DECIMAL(10,2) NOT NULL DEFAULT 0.00。
- `pay_amount` DECIMAL(10,2) NOT NULL。
- `pay_type` TINYINT：1余额，2微信，3支付宝。
- `status` TINYINT NOT NULL：
  - 0待支付，1已支付待接单，2配送中，3已完成，4已取消，5退款中，6已退款。
- `remark` VARCHAR(255)。
- `created_at` DATETIME NOT NULL。
- `paid_at` DATETIME。
- `finished_at` DATETIME。

### 7）订单明细表 `order_item`
- `id` BIGINT PK AUTO_INCREMENT。
- `order_id` BIGINT NOT NULL FK -> `orders.id`。
- `dish_id` BIGINT NOT NULL FK -> `dish.id`。
- `dish_name` VARCHAR(100) NOT NULL（快照）。
- `dish_price` DECIMAL(10,2) NOT NULL（快照）。
- `quantity` INT NOT NULL。
- `subtotal` DECIMAL(10,2) NOT NULL。

### 8）收货地址表 `address`
- `id` BIGINT PK AUTO_INCREMENT。
- `user_id` BIGINT NOT NULL FK -> `user.id`。
- `consignee` VARCHAR(50) NOT NULL。
- `phone` VARCHAR(20) NOT NULL。
- `province` VARCHAR(50) NOT NULL。
- `city` VARCHAR(50) NOT NULL。
- `district` VARCHAR(50) NOT NULL。
- `detail` VARCHAR(255) NOT NULL。
- `is_default` TINYINT DEFAULT 0。
- `created_at` DATETIME NOT NULL。
- `updated_at` DATETIME NOT NULL。

### 9）评价表 `review`
- `id` BIGINT PK AUTO_INCREMENT。
- `order_id` BIGINT NOT NULL FK -> `orders.id`。
- `user_id` BIGINT NOT NULL FK -> `user.id`。
- `merchant_id` BIGINT NOT NULL FK -> `merchant.id`。
- `dish_id` BIGINT NULL FK -> `dish.id`（按业务可选）。
- `score` TINYINT NOT NULL CHECK(score BETWEEN 1 AND 5)。
- `content` VARCHAR(500)。
- `created_at` DATETIME NOT NULL。

### 10）管理员表 `admin`
- `id` BIGINT PK AUTO_INCREMENT。
- `username` VARCHAR(50) UNIQUE NOT NULL。
- `password_hash` VARCHAR(255) NOT NULL。
- `role` VARCHAR(30) NOT NULL：super_admin / auditor / operator。
- `status` TINYINT DEFAULT 1。
- `last_login_at` DATETIME。
- `created_at` DATETIME NOT NULL。

---

# 第六章 系统详细设计与实现

## 6.1 系统开发环境搭建

### 6.1.1 后端环境搭建
- 语言与框架：Java 17 + Spring Boot 3.x（或 Node.js + Express，二选一保持一致）。
- 核心依赖：MyBatis/JPA、JWT、Redis、Swagger/OpenAPI。
- 工程结构：controller、service、mapper/repository、entity、dto、config、common。
- 统一规范：统一返回体、全局异常处理、参数校验、日志与审计。

### 6.1.2 前端环境搭建
- 用户端：Vue3 + Vite + Element Plus（PC）或 UniApp（移动端）。
- 商家端/管理端：Vue3 + Pinia + Vue Router + Axios。
- 关键配置：请求拦截器（token 注入）、响应拦截器（统一错误处理）、环境变量（dev/test/prod）。

### 6.1.3 数据库环境搭建
- MySQL 8.0，字符集 `utf8mb4`。
- 建库建表脚本、索引脚本、初始数据脚本分离管理。
- 使用 Flyway/Liquibase 进行版本化迁移。
- Redis 用于验证码、会话缓存、热点数据缓存。

## 6.2 用户模块设计与实现

### 6.2.1 用户注册与登录实现
- 注册：手机号/邮箱 + 验证码 + 密码强度校验。
- 登录：账号密码登录，校验成功后签发 JWT（含用户ID、角色、过期时间）。
- 安全：密码哈希（BCrypt）、登录限流、防暴力破解。

### 6.2.2 用户信息管理实现
- 支持查看与修改昵称、头像、手机号等。
- 对敏感字段（手机号）做二次验证。
- 接口层使用 DTO，避免实体直接暴露。

### 6.2.3 地址管理实现
- 支持地址新增、编辑、删除、查询。
- 设置默认地址时自动取消其他默认地址。
- 下单时优先读取默认地址，若无则提示用户选择。

## 6.3 菜品与店铺模块设计与实现

### 6.3.1 店铺展示功能实现
- 首页按评分、销量、距离等维度展示店铺。
- 支持筛选（起送价、配送费、品类）。
- 使用分页和缓存提升查询性能。

### 6.3.2 菜品分类展示实现
- 按店铺加载分类列表及分类下菜品。
- 分类支持排序字段控制前端展示顺序。

### 6.3.3 菜品详情功能实现
- 展示菜品图文详情、月售、库存、价格、评价摘要。
- 对下架或库存不足商品进行下单拦截。

### 6.3.4 搜索功能实现
- 支持按店铺名、菜品名关键词搜索。
- 支持联想词、历史记录（可用 Redis 存储近期关键词）。

## 6.4 购物车模块设计与实现

### 6.4.1 添加商品到购物车
- 校验菜品状态、库存与商家营业状态。
- 若购物车已存在同菜品则数量累加，否则新增条目。

### 6.4.2 修改商品数量
- 支持增减数量，最小值 1。
- 减少至 0 时自动删除该条目。

### 6.4.3 删除购物车商品
- 支持单条删除与批量清空。
- 删除后实时返回最新金额统计。

### 6.4.4 结算功能实现
- 结算前二次校验价格、库存与营业状态。
- 计算商品总额、配送费、优惠金额、应付金额。
- 结算成功后生成订单并清理对应购物车项。

## 6.5 订单模块设计与实现

### 6.5.1 提交订单
- 前端提交地址、备注、购物车项。
- 后端事务处理：锁库存、写入订单与明细、扣减库存。

### 6.5.2 订单支付
- 接入第三方支付（微信/支付宝）或余额支付。
- 支付回调校验签名并幂等更新订单状态。

### 6.5.3 订单状态流转
- 状态链：待支付 → 已支付待接单 → 商家已接单/配送中 → 已完成。
- 取消链：待支付可取消；已支付在规则内可申请退款。
- 使用状态机或枚举统一管理状态转换规则。

### 6.5.4 订单查询与取消
- 用户可按状态分页查询历史订单。
- 支持订单详情查看、再次购买、取消订单。
- 取消后需回滚库存并记录操作日志。

## 6.6 商家管理模块设计与实现

### 6.6.1 菜品管理
- 商家可新增、编辑、删除、上下架菜品。
- 支持批量操作、库存预警。

### 6.6.2 订单接收与处理
- 商家端实时接收新订单通知。
- 支持接单、拒单（附原因）、标记配送状态。

### 6.6.3 店铺管理
- 维护店铺基础信息、营业时间、配送规则。
- 支持临时打烊、节假日公告。

### 6.6.4 数据统计
- 统计日/周/月销售额、订单量、客单价、热销菜品。
- 结果可图表化展示并支持导出。

## 6.7 管理员模块设计与实现

### 6.7.1 用户管理
- 查看用户列表、状态管理、违规处理。

### 6.7.2 商家审核管理
- 审核商家入驻资料，支持通过/驳回并记录原因。

### 6.7.3 菜品分类管理
- 维护平台统一分类，支持增删改查与排序。

### 6.7.4 订单监督管理
- 全局查看订单流转，处理异常订单与投诉工单。

### 6.7.5 公告发布管理
- 发布平台公告、活动通知、规则更新。
- 支持定时发布与撤回。

---

## 本章小结
本章从数据库概念设计、逻辑结构设计到关键业务模块实现路径进行了完整描述。通过主外键约束、状态机控制、事务与幂等设计，可保障系统在高并发场景下的一致性与可维护性；通过用户端、商家端、管理端分层设计，能够支撑外卖平台核心业务闭环。
